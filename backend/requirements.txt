# BidOps AI — dependencies, added incrementally as each milestone needed them.
# Grouped roughly by the milestone that introduced them (see 99_DECISIONS_LOG.md
# for the reasoning behind each library choice).

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic-settings==2.5.2
sqlalchemy==2.0.35
psycopg2-binary==2.9.10
alembic==1.13.3
bcrypt==4.2.0
PyJWT==2.9.0
email-validator==2.2.0
python-multipart==0.0.12
pypdf==5.0.1
pdf2image==1.17.0
pytesseract==0.3.13
python-docx==1.1.2
openai==1.109.1
google-genai==2.12.1

# Google Authentication (Phase 2) -- ID token verification only (no OAuth
# flow/redirect handling needed server-side; the frontend uses Google
# Identity Services to obtain the token). Version range matches what
# google-genai already requires (>=2.48.1,<2.56.0) so both dependencies
# resolve to one shared google-auth install, not two conflicting pins.
google-auth>=2.48.1,<2.56.0

# Document storage on Cloud Run (Phase 3) -- only actually used when
# STORAGE_BACKEND=gcs (app/core/storage.py imports it lazily, inside the
# one function that needs it); harmless to always install since it's a
# small, dependency-light package.
google-cloud-storage==2.18.2

# Testing (M11.5) — committed offline verification harness for the LLM
# provider layer (see tests/agents/test_llm_client.py).
pytest==8.3.4
pytest-asyncio==0.25.2

# Rate limiting (RC-2 audit finding H-2) — protects account creation and
# every LLM-cost-incurring endpoint from unlimited/free abuse.
slowapi==0.1.10

# Contact form email delivery (Contact Form Backend feature) -- Resend's
# official SDK. No prior email-sending capability existed in this
# codebase; Resend was chosen (no further comparison needed per the
# governing decision) as a transactional email API reachable over plain
# outbound HTTPS, which is what Cloud Run's egress path supports cleanly
# (unlike SMTP on 25/587, which can be finicky from some cloud egress
# paths -- see docs/DEPLOYMENT.md).
resend==2.36.0

# Tender multi-document support (real CPPP tender validation gap) --
# spreadsheet parsing for tech.xls/BOQ.xls-style tender attachments.
# openpyxl reads the modern OOXML .xlsx format; xlrd is the only
# maintained pure-Python reader for the legacy binary .xls format
# openpyxl dropped support for -- both mature, widely used, lightweight
# (no compiled extensions, no heavy transitive dependency tree).
openpyxl==3.1.5
xlrd==2.0.1

# Test-only: generates a genuinely valid, pypdf-readable PDF with real
# extractable text for tests/test_tender_multi_document.py -- a fake
# '%PDF'-prefixed byte string wouldn't exercise extract_pdf_pages() at
# all. Not imported anywhere outside the tests/ directory.
reportlab==4.5.1
